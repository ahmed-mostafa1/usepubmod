(function (context, modName, definition) {
  // if node

  if (typeof module !== "undefined" && module.exports) {
    module.exports = definition();
  }

  // if window
  // window obj
  else {
    context[modName] = definition();
  }
})(this, "shoppingCartCustomMod", function () {
  // api
  const api = {
    name: "Shopping Cart",
    description: "add items to the Card",
  };
  return api;
});
